{
  "name": "rewards",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "main": "index.mjs",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "2csv": "json2csv -i events.json -o events.csv && json2csv -i events-points.json -o events-points.csv && json2csv -i oracles.json -o oracles.csv && json2csv -i oracles-points.json -o oracles-points.csv && json2csv -i distributions.json -o distributions.csv",
    "start": "node index.mjs"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@taikai/dappkit": "^2.0.0-alpha.2",
    "bignumber.js": "^9.0.2",
    "json2csv": "^5.0.7"
  }
}
